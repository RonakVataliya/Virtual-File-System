# Virtual-File-System
A Linux based virtual file system entirely in userspace. A layered architecture with a disk image backend, inode-based metadata, directory hierarchy and CLI for interacting with virtual file tree. 
