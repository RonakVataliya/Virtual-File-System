#pragma once

// This module will provide a command-line interface (CLI) on top
// of the virtual file system.
// It will parse user commands (like ls, mkdir, touch, cat, etc.)
// and call corresponding functions from the fs layer.